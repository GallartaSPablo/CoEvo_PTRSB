# Raw simulation output (regenerate with src/run_coevolution_networks.sh).
# files/networks/temporal/ alone is >200 GB at the paper's defaults -- far
# beyond what git/GitHub can host.
/files/

# Per-species dataframe built from files/ by figures.ipynb (~170 MB, over
# GitHub's 100 MB file limit). Regenerated by running the notebook.
/dataframes/

# Compiled binary (build it with src/run_coevolution_networks.sh or the gcc
# command in src/coevolution_antagonistic_networks.c)
src/coevolution_antagonistic_networks

# OS / editor junk
.DS_Store
.ipynb_checkpoints/
__pycache__/
*.pyc
